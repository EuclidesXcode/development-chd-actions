import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#daa520', // Cor primária
    },
    secondary: {
      main: '#daa520', // Cor secundária
    },
  },
});

export default theme;